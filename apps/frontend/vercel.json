{
  "buildCommand": "cd ../.. && npm run typecheck -w packages/shared && npm run build -w apps/frontend",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://agentic-space-backend.onrender.com/api/$1" }
  ]
}
